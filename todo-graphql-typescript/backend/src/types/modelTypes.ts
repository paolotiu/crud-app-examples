export interface ItemAndCategoryIDs {
  item_id: string;
  category_id: string;
}

export interface ItemModel {
  id: string;
  name: string;
  price: number;
}

export interface CategoryModel {
  id: string;
  name: string;
}
